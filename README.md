# tollbooth

A security gateway for AI agents: a transparent MCP proxy that enforces policy, DLP, and audit on every tool call and result.

## Setup

```bash
uv sync
```

## Usage

```bash
uv run tollbooth --help
```

## Development

```bash
# Run tests
uv run pytest

# Lint
uv run ruff check .
```
